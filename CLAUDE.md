#Project

This Project is beeing made for a Docker container im my ubuntu server home server, but the development is on a Windows desktop enviroment (here).

After code changes, i will commit and push, then enter my home server directory 'vini@homenas:~/servidor-docker/money_management$' and pull all of the changes.
 
The deploy method for code updates is Always 'vini@homenas:~/servidor-docker$ docker compose up -d --build --force-recreate money-management'